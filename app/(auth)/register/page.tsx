import { RegisterForm } from "@/components/auth/auth/register-form";

const RegisterPage = async () => {
  return (
    <div className="flex items-center justify-center w-full h-[160vh]">
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;
