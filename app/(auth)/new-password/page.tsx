import { NewPasswordForm } from "@/components/auth/auth/new-password-form";

const NewPasswordPage = async () => {
  return (
    <div className="flex items-center justify-center w-full h-[80vh]">
      <NewPasswordForm/>
    </div>
  );
};

export default NewPasswordPage;
